---
phase: 02-openapi-error
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - packages/openapi/test/cross-runtime.test.ts
  - packages/middleware/test/cross-runtime.test.ts
  - .planning/phases/02-openapi-error/02-03-PLAN.md
autonomous: true
requirements: [OPENAPI-01, OPENAPI-02, OPENAPI-03, OPENAPI-04, ERR-01, ERR-02]

must_haves:
  truths:
    - "All packages pass cross-runtime CI (Node.js, Bun, Cloudflare Workers)"
  artifacts:
    - path: "packages/openapi/test/cross-runtime.test.ts"
      provides: "Cross-runtime verification for @honoforge/openapi"
      contains: "describe('cross-runtime'"
    - path: "packages/middleware/test/cross-runtime.test.ts"
      provides: "Cross-runtime verification for @honoforge/middleware"
      contains: "describe('cross-runtime'"
  key_links:
    - from: "packages/openapi/test/cross-runtime.test.ts"
      to: "packages/openapi/src/index.ts"
      via: "import all exports"
      pattern: "from '../src/index.js'"
    - from: "packages/middleware/test/cross-runtime.test.ts"
      to: "packages/middleware/src/index.ts"
      via: "import all exports"
      pattern: "from '../src/index.js'"
---

<objective>
Verify cross-runtime compatibility for both `@honoforge/openapi` and `@honoforge/middleware` packages.

Purpose: Ensure both packages work correctly across Node.js, Bun, and Cloudflare Workers runtimes — a core requirement for honoforge's zero-dependency, cross-runtime philosophy.

Output: Cross-runtime test files that verify imports and basic functionality work on all target runtimes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md — Runtime Support Matrix section
@.planning/research/SUMMARY.md — Cross-runtime testing guidance
@.planning/phases/01-foundation/01-01-SUMMARY.md — Build pipeline established in Phase 1
@packages/openapi/package.json
@packages/middleware/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cross-runtime verification tests for both packages</name>
  <files>packages/openapi/test/cross-runtime.test.ts, packages/middleware/test/cross-runtime.test.ts</files>
  <read_first>
    - packages/openapi/src/index.ts — All exports to verify importability
    - packages/middleware/src/index.ts — All exports to verify importability
    - .planning/research/STACK.md — Runtime support matrix, cross-runtime guidance
  </read_first>
  <action>
    Create cross-runtime test files that verify both packages import and function correctly on all target runtimes. These tests verify that:
    1. All exports are importable without runtime errors
    2. Core functions execute without throwing
    3. Type inference works (compile-time check, verified by typecheck)

    **packages/openapi/test/cross-runtime.test.ts:**
    - Import all exports from `../src/index.js`: `zodToOpenAPI`, `extendZodWithOpenAPI`, `createResponse`, `okResponse`, `createdResponse`, `errorResponse`, `extractRouteMetadata`, `listRoutes`, `getRouteByPath`, `generateOpenAPIDoc`, `serveOpenAPIDoc`, `generateOpenAPIDocYAML`
    - Test that `zodToOpenAPI(z.string())` executes without throwing
    - Test that `okResponse({ test: true })` returns expected structure
    - Test that `createResponse('hello', 200)` returns expected structure
    - Test that `formatError` (imported via re-export if applicable) works
    - Use `describe('cross-runtime', ...)` block

    **packages/middleware/test/cross-runtime.test.ts:**
    - Import all exports from `../src/index.js`: `errorHandler`, `formatError`, `formatHTTPError`, `formatProblemDetails`, `ProblemDetails`, `ProblemDetailsOptions`
    - Test that `formatError(new Error('test'))` executes without throwing
    - Test that `formatProblemDetails({ status: 200, title: 'OK' })` returns expected structure
    - Test that `errorHandler` is a function (middleware is importable)
    - Use `describe('cross-runtime', ...)` block

    These tests are intentionally minimal — they verify importability and basic execution. Full behavioral testing is covered by the per-feature test files in Plans 02-01 and 02-02.

  </action>
  <verify>
    <automated>cd packages/openapi && pnpm vitest run test/cross-runtime.test.ts && cd packages/middleware && pnpm vitest run test/cross-runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/openapi/test/cross-runtime.test.ts imports all public exports without errors
    - packages/openapi/test/cross-runtime.test.ts contains `describe('cross-runtime'`
    - packages/middleware/test/cross-runtime.test.ts imports all public exports without errors
    - packages/middleware/test/cross-runtime.test.ts contains `describe('cross-runtime'`
    - `cd packages/openapi && pnpm vitest run test/cross-runtime.test.ts` exits 0
    - `cd packages/middleware && pnpm vitest run test/cross-runtime.test.ts` exits 0
  </acceptance_criteria>
  <done>Cross-runtime tests verify both packages import and execute correctly on all target runtimes. Tests are minimal importability checks — full behavior tested in per-feature test files.</done>
</task>

<task type="auto">
  <name>Task 2: Run full verification suite for both packages</name>
  <files>.planning/phases/02-openapi-error/02-03-SUMMARY.md</files>
  <read_first>
    - packages/openapi/package.json — Verify all scripts exist
    - packages/middleware/package.json — Verify all scripts exist
  </read_first>
  <action>
    Run the complete verification suite for both packages to confirm everything passes:

    1. `pnpm install` at root — ensure all workspace dependencies are linked
    2. `cd packages/openapi && pnpm build` — verify build succeeds
    3. `cd packages/middleware && pnpm build` — verify build succeeds
    4. `cd packages/openapi && pnpm vitest run` — all openapi tests pass
    5. `cd packages/middleware && pnpm vitest run` — all middleware tests pass
    6. `cd packages/openapi && pnpm typecheck` — TypeScript compilation succeeds
    7. `cd packages/middleware && pnpm typecheck` — TypeScript compilation succeeds
    8. `pnpm build` at root — full monorepo build succeeds
    9. `pnpm test` at root — full monorepo test suite passes

    Record results in the summary file.

  </action>
  <verify>
    <automated>pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` at root exits 0
    - `pnpm test` at root exits 0
    - All packages produce dist/ with .mjs, .cjs, .d.mts, .d.cts files
    - No TypeScript compilation errors across all packages
  </acceptance_criteria>
  <done>Full monorepo build and test suite passes. Both packages produce valid dual ESM/CJS artifacts with type declarations.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                            |
| ----------------------------- | -------------------------------------------------------------------------------------- |
| Test code → Runtime execution | Cross-runtime tests import and execute package code — no external I/O or network calls |

## STRIDE Threat Register

| Threat ID | Category | Component           | Disposition | Mitigation Plan                                                                                                      |
| --------- | -------- | ------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| T-02-07   | N/A      | cross-runtime tests | accept      | Tests are pure import/execution checks with no external dependencies, network calls, or file I/O. No attack surface. |

</threat_model>

<verification>
- `pnpm build` at root exits 0 — all packages build successfully
- `pnpm test` at root exits 0 — all tests pass across all packages
- Both packages produce dist/ with .mjs, .cjs, .d.mts, .d.cts
- Cross-runtime tests pass on Node.js (Bun/Workers verification documented for CI matrix)
- All six requirement IDs (OPENAPI-01 through OPENAPI-04, ERR-01, ERR-02) addressed
</verification>

<success_criteria>

- Both packages build cleanly with dual ESM/CJS + DTS output
- All tests pass across both packages (20+ total tests)
- Cross-runtime verification confirms importability on target runtimes
- Full monorepo build and test suite passes
- Phase 2 goal achieved: working `@honoforge/openapi` and `@honoforge/middleware` (error handling) packages
  </success_criteria>

<output>
After completion, create `.planning/phases/02-openapi-error/02-03-SUMMARY.md`
</output>
