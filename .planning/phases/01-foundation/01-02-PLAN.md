---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/http-status-codes.ts
  - packages/core/src/http-status-phrases.ts
  - packages/core/test/http-status.test.ts
autonomous: true
requirements: [CORE-01, CORE-02]
must_haves:
  truths:
    - "HTTP status codes are importable from @honoforge/core as typed numeric constants"
    - "HTTP status phrases are importable from @honoforge/core as typed string constants"
    - "JSDoc RFC references are preserved on each constant"
  artifacts:
    - path: "packages/core/src/http-status-codes.ts"
      provides: "Typed numeric HTTP status code constants"
      contains: "export const OK = 200"
    - path: "packages/core/src/http-status-phrases.ts"
      provides: "Typed string HTTP status phrase constants"
      contains: 'export const OK = "OK"'
    - path: "packages/core/src/index.ts"
      provides: "Barrel exports for status codes and phrases"
      exports: ["* from http-status-codes", "* from http-status-phrases"]
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/http-status-codes.ts"
      via: "re-export"
      pattern: "export \\* from.*http-status-codes"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/http-status-phrases.ts"
      via: "re-export"
      pattern: "export \\* from.*http-status-phrases"
---

<objective>
Migrate existing HTTP status codes and phrases from `src/` into `packages/core/src/` with full JSDoc RFC references, typed constants, and test coverage.

Purpose: Preserve existing HTTP status utilities while moving them into the proper package structure. These are the first concrete exports of @honoforge/core.
Output: Migrated HTTP status files in packages/core/ with tests verifying importability and type correctness.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/STACK.md
@src/http-status-codes.ts
@src/http-status-phrases.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from source files. -->

From src/http-status-codes.ts (migration source):
```typescript
// Pattern: typed numeric constants with JSDoc
/**
 * Official Documentation @ https://tools.ietf.org/html/rfc7231#section-6.3.1
 */
export const OK = 200;
export const NOT_FOUND = 404;
export const INTERNAL_SERVER_ERROR = 500;
// ... 60+ constants, all `export const NAME = number;`
```

From src/http-status-phrases.ts (migration source):
```typescript
// Pattern: typed string constants with JSDoc
/**
 * Official Documentation @ https://tools.ietf.org/html/rfc7231#section-6.3.1
 */
export const OK = "OK";
export const NOT_FOUND = "Not Found";
export const INTERNAL_SERVER_ERROR = "Internal Server Error";
// ... 60+ constants, all `export const NAME = "string";`
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate HTTP status codes and phrases into packages/core/src/</name>
  <files>packages/core/src/http-status-codes.ts, packages/core/src/http-status-phrases.ts, packages/core/src/index.ts</files>
  <read_first>
    - src/http-status-codes.ts (source file — copy content exactly)
    - src/http-status-phrases.ts (source file — copy content exactly)
    - packages/core/src/index.ts (created in Plan 01 — update with re-exports)
  </read_first>
  <action>
    1. Copy `src/http-status-codes.ts` to `packages/core/src/http-status-codes.ts` — preserve ALL content exactly (JSDoc comments, constants, @deprecated annotations). Do not modify the file content.

    2. Copy `src/http-status-phrases.ts` to `packages/core/src/http-status-phrases.ts` — preserve ALL content exactly.

    3. Verify `packages/core/src/index.ts` already contains the re-exports for http-status-codes.js and http-status-phrases.js (created in Plan 01). No modification needed.

    4. Delete the old files at `src/http-status-codes.ts` and `src/http-status-phrases.ts` after confirming the copies are identical:
       ```bash
       diff src/http-status-codes.ts packages/core/src/http-status-codes.ts
       diff src/http-status-phrases.ts packages/core/src/http-status-phrases.ts
       rm src/http-status-codes.ts src/http-status-phrases.ts
       ```
  </action>
  <verify>
    <automated>diff src/http-status-codes.ts packages/core/src/http-status-codes.ts 2>/dev/null; echo "exit: $?" || diff /dev/null packages/core/src/http-status-codes.ts >/dev/null 2>&1 && grep -c "export const" packages/core/src/http-status-codes.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/http-status-codes.ts contains `export const OK = 200`
    - packages/core/src/http-status-phrases.ts contains `export const OK = "OK"`
    - packages/core/src/http-status-codes.ts contains `export const NOT_FOUND = 404`
    - packages/core/src/http-status-phrases.ts contains `export const NOT_FOUND = "Not Found"`
    - packages/core/src/http-status-codes.ts contains `export const IM_A_TEAPOT = 418`
    - packages/core/src/index.ts contains `export * from './http-status-codes.js'` (pre-declared in Plan 01)
    - packages/core/src/index.ts contains `export * from './http-status-phrases.js'` (pre-declared in Plan 01)
    - Old src/http-status-codes.ts no longer exists
    - Old src/http-status-phrases.ts no longer exists
  </acceptance_criteria>
  <done>HTTP status codes and phrases migrated into packages/core/ with all JSDoc RFC references preserved. Index.ts re-exports both modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests for HTTP status codes and phrases</name>
  <files>packages/core/test/http-status.test.ts</files>
  <read_first>
    - packages/core/src/http-status-codes.ts (verify constant names and values)
    - packages/core/src/http-status-phrases.ts (verify constant names and values)
    - test/api-snapshot.test.ts (existing test pattern — follow same vitest import style)
  </read_first>
  <behavior>
    - Test 1: OK code equals 200 (numeric type)
    - Test 2: NOT_FOUND code equals 404 (numeric type)
    - Test 3: INTERNAL_SERVER_ERROR code equals 500 (numeric type)
    - Test 4: OK phrase equals "OK" (string type)
    - Test 5: NOT_FOUND phrase equals "Not Found" (string type)
    - Test 6: All codes are numbers (typeof check on exported constants)
    - Test 7: All phrases are strings (typeof check on exported constants)
    - Test 8: Code and phrase counts match (same number of constants in both files)
  </behavior>
  <action>
    1. Create `packages/core/test/http-status.test.ts` with Vitest tests:
       ```typescript
       import { describe, it, expect } from 'vitest'
       import * as codes from '../src/http-status-codes.js'
       import * as phrases from '../src/http-status-phrases.js'

       describe('HTTP status codes', () => {
         it('OK equals 200', () => {
           expect(codes.OK).toBe(200)
         })
         it('NOT_FOUND equals 404', () => {
           expect(codes.NOT_FOUND).toBe(404)
         })
         it('INTERNAL_SERVER_ERROR equals 500', () => {
           expect(codes.INTERNAL_SERVER_ERROR).toBe(500)
         })
         it('all exports are numbers', () => {
           for (const [key, value] of Object.entries(codes)) {
             expect(typeof value).toBe('number')
           }
         })
       })

       describe('HTTP status phrases', () => {
         it('OK equals "OK"', () => {
           expect(phrases.OK).toBe('OK')
         })
         it('NOT_FOUND equals "Not Found"', () => {
           expect(phrases.NOT_FOUND).toBe('Not Found')
         })
         it('all exports are strings', () => {
           for (const [key, value] of Object.entries(phrases)) {
             expect(typeof value).toBe('string')
           }
         })
       })

       describe('codes and phrases alignment', () => {
         it('same number of constants', () => {
           expect(Object.keys(codes).length).toBe(Object.keys(phrases).length)
         })
       })
       ```

    2. Run `cd packages/core && pnpm vitest run test/http-status.test.ts` to verify all tests pass.
  </action>
  <verify>
    <automated>cd packages/core && pnpm vitest run test/http-status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/test/http-status.test.ts exists
    - Test file imports from '../src/http-status-codes.js' and '../src/http-status-phrases.js'
    - `pnpm vitest run test/http-status.test.ts` exits 0 with all tests passing
    - Tests verify OK=200, NOT_FOUND=404, INTERNAL_SERVER_ERROR=500
    - Tests verify OK="OK", NOT_FOUND="Not Found"
    - Tests verify typeof all code exports is 'number'
    - Tests verify typeof all phrase exports is 'string'
    - Tests verify code count equals phrase count
  </acceptance_criteria>
  <done>Tests pass verifying HTTP status codes are correct numeric values, phrases are correct strings, and both files have matching constant counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source file → package copy | Migration must preserve exact content — any modification risks losing RFC references |
| Consumer → status constants | Constants are read-only values — no untrusted input flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Integrity | http-status-codes.ts migration | mitigate | Use `diff` to verify exact copy before deleting originals; tests verify all constants match expected values |
| T-01-05 | Tampering | http-status-phrases.ts | accept | Static constants with no external input — no tampering vector |
</threat_model>

<verification>
- [ ] http-status-codes.ts migrated with all 60+ constants and JSDoc RFC references
- [ ] http-status-phrases.ts migrated with all 60+ constants and JSDoc RFC references
- [ ] packages/core/src/index.ts re-exports both modules
- [ ] Old src/ files removed after verification
- [ ] Tests pass: codes are correct numbers, phrases are correct strings
- [ ] Build still passes after migration (`cd packages/core && pnpm build`)
</verification>

<success_criteria>
- HTTP status codes importable from @honoforge/core as typed numeric constants (CORE-01)
- HTTP status phrases importable from @honoforge/core as typed string constants (CORE-02)
- All JSDoc RFC references preserved from original files
- Tests verify correctness of key constants and type alignment
- Build pipeline still produces valid dist/ output
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
